{
  "name": "@witchwatch/acquisition",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "check": "tsc -p tsconfig.json --pretty false",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "3.2.4"
  }
}
